apiVersion: v1
kind: ConfigMap
metadata:
  name: my-grafana-dashboard
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  my-dashboard.json: |-
    {
      // your dashboard JSON content here
    }
